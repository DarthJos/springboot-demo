package com.example.demo.utils;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class AssymetricCodification {
    private static final Logger log = LogManager.getLogger(AssymetricCodification.class);

    public String encryptAsymmetric() {
        return "encryptAsymmetric done...";
    }
}
